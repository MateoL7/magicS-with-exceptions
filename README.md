# magicS-with-exceptions is a project where a magic square is created. The goal is to create an square with odd dimensions and that the 
sum between every row, column and diagonal have the same result. 
